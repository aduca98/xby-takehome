overwrite: true
schema: ./src/**/*.graphql
generates:
    src/core/surfaces/graphql/generated/types.ts:
        plugins:
            - "typescript"
            - "typescript-resolvers"
    ../client/src/api/graphql/generated/types.ts:
        plugins:
            - "typescript"
            - "typescript-resolvers"
