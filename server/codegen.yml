overwrite: true
schema: "src/**/*.graphql"
config:
    scalars:
        Date: Date
generates:
    src/core/surfaces/graphql/generated/types.ts:
        plugins:
            - "typescript"
            - "typescript-resolvers"
