type PublicUser {
    name: String
    firstName: String
    lastName: String
    profileUrl: String
    answers: [UserAnswer]
}

extend type Query {
    me: User!
    getByUsername(username: String!): PublicUser
}
