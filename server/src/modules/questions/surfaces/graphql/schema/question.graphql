enum QuestionType {
    MultipleChoice
    ShortAnswer
}

type QuestionOption {
    id: String!
    label: String!
    value: String!
}

type Question {
    id: String!
    title: String!
    type: QuestionType!
    required: Boolean!
    options: [QuestionOption]
}
