enum QuestionType {
    MultipleChoice
    ShortAnswer
}

type QuestionOption {
    id: String!
    label: String!
    value: String!
}

type BaseQuestion {
    id: String!
    title: String!
    type: QuestionType!
    required: Boolean!
    options: [QuestionOption]
}
