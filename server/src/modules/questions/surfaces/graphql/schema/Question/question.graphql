enum QuestionType {
    MultipleChoice
    ShortAnswer
}

type QuestionOption {
    id: String!
    label: String!
    value: String!
}

type Question {
    title: String!
    type: QuestionType!
    required: Bool!
    options: [QuestionOption]
}
